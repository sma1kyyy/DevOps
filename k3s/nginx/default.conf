server {
    listen 80;
    server_name _;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/app.sock;
    }

    location = /nginx_status {
        stub_status;
        allow all;
    }
}
